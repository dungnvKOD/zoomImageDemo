<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:id="@+id/container"
             android:layout_width="match_parent"
             android:layout_height="match_parent">

    <ImageView
            android:id="@+id/expanded_image"
            android:background="#ff99"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="invisible"
            android:contentDescription="@string/app_name"/>

    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:orientation="vertical"
                  android:padding="16dp">

        <ImageView
                android:id="@+id/thumb_button_1"
                android:layout_width="100dp"
                android:layout_height="75dp"
                android:layout_marginRight="1dp"
                android:src="@drawable/aaa"
                android:scaleType="centerCrop"
                android:contentDescription="@string/app_name"/>

    </LinearLayout>

    <!-- ImageView ẩn ban đầu này sẽ giữ phiên bản mở rộng / thu phóng của những hình ảnh trên. Nếu không có biến đổi được áp dụng, nó chiếm toàn bộ màn.
     Để đạt được hoạt ảnh "thu phóng",
      giới hạn của chế độ xem này được hoạt ảnh từ giới hạn của nút hình thu nhỏ ở trên, đến phần cuối cùng được đặt ra giới hạn
         -->



</FrameLayout>